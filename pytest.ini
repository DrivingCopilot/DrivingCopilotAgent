[pytest]
asyncio_mode = auto
# 통합 테스트는 실 인프라(MCP 서버 등)가 필요하므로 기본 실행에서 제외한다.
# 실행하려면: pytest -m integration
addopts = -m "not integration"
markers =
    integration: 실 MCP 서버가 필요한 통합 테스트 (기본 제외, -m integration 으로 실행)
